server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        # Falls back to index.html for any unmatched route, so
        # React Router can handle client-side routes like /patients
        # or /profile when the browser is refreshed or linked directly
        try_files $uri $uri/ /index.html;
    }
}