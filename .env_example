# Copy this file to ".env" (same folder as docker-compose.yml) and
# fill in your real values. The ".env" file itself is gitignored and
# will NEVER be committed — only this .example template is.

SECRET_KEY=paste-your-real-secret-key-here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60