# Frontend — see the top-level README.md

Setup, PostgreSQL configuration, and full instructions live in the
root `README.md` (one level up). This folder is just the React app.

Quick start (after the backend is running):
```
npm install
npm run dev
```
