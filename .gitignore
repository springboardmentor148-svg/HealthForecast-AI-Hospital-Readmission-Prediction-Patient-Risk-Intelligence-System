# ===== Backend (Python) =====
backend/venv/
backend/__pycache__/
backend/**/__pycache__/
backend/.env
*.pyc

# Docker Compose secrets file — NEVER commit this. Copy .env.example
# to .env and fill in real values locally / on the server instead.
.env

# Model files live at project root (models/), not inside backend/ —
# uncomment if you don't want to track them in Git (they're currently
# small enough — ~712KB — that tracking them is fine)
# models/*.pkl

# ===== Frontend (Node/React) =====
frontend/node_modules/
frontend/dist/
frontend/.vite/

# ===== General =====
.DS_Store
*.log
.vscode/