import React from 'react';

export default function ModelPerformancePage() {
  return (
    <div className="space-y-4">
      <h1 className="text-[20px] font-semibold text-txt-primary">AI Model Performance Console</h1>
      <p className="text-[14px] text-txt-muted">Model training run controllers, metrics evaluations, and decision threshold optimizations.</p>
    </div>
  );
}
